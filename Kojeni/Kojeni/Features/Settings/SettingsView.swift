import SwiftUI

struct SettingsView: View {
    var body: some View {
        NavigationStack {
            Text("Nastavení")
                .navigationTitle("Nastavení")
        }
    }
}

#Preview {
    SettingsView()
}
