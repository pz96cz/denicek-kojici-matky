import SwiftUI

struct HistoryView: View {
    var body: some View {
        NavigationStack {
            Text("Historie")
                .navigationTitle("Historie")
        }
    }
}

#Preview {
    HistoryView()
}
